# AstraChat Vite

AstraChat has been migrated from a monolithic static HTML page into a Vite application.

## Commands

```bash
npm install
npm run dev
npm run build
npm run preview
```

Source lives in `src/`; PWA files and icons live in `public/`.
